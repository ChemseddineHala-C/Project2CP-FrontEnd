# BahrFresh
Note: to try the you just need the api key of google map which you must put in
./android/app/build.gradle.kts in the line 22 inside the "THE_API_KEY_OF_GOOGLE_MAPS" , replace with it.

the app will work without api key but you can't try the best part of it without the api_key
